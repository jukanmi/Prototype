# 콤보 덱 스킬 테이블 (16종)

> 4직업 × 4종 = 16종. **에셋 1개 = 스킬 1개** (`Assets/Data/Skills/SK_*.asset`).
> 각 `Ally`가 자기 직업 4장을 `equipped`에 들고 있고, `BulletTimeController.BuildDeckFromParty()`가 이걸 모아 **16장 덱**을 만든다.
> 불릿타임마다 손패 **4장**(`Hand.Size`)까지 채우고, 손패 자체가 콤보 실행 순서다.
>
> 이 문서는 실제 에셋 값을 그대로 옮긴 것이다. 값을 고치려면 에셋을 고치고 여기를 다시 맞춘다.

---

## 0. 상태 전이 축

콤보는 `CombatState` 사슬 위에서 굴러감. 전이 판정은 `CombatStateRules`가 단독 관리.

```
Neutral ──모으기──> LightHit ──띄우기──> AerialHit ──밀치기──> Knockback ──벽접촉──> WallBound
                                            │                      │              │
                                        공격기(유지)                └──── 바닥접촉 ─┴──> Down ──> Getup(무적) ──> Neutral
                                                                                          ↑ canOtg 로만 관통
```

- 공중 피격 중에는 무엇을 맞아도 `AerialHit` 유지. **단 `Knockback` 요청만 통과** → 벽바운드 연계가 성립.
- `MaxAirHit = 10` 넘기면 띄우기 무시하고 낙하. 피격마다 중력 `+0.15`, 상한 `2.0`.
- `CanChain`: `require == AerialHit`이면 `AerialHit / WallBound / Knockback` 전부 허용.

**정석 4슬롯 체인**: `모으기(Gather) → 띄우기(Launcher) → 공격기(Strike) → 밀치기(Push)`

---

## 1. 탱커 (Tanker) — role 0

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | radius | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|
| SK_TK1A | 소용돌이 베기 | Gather | Neutral → LightHit | None | 3.5 | 0.2 / 0.25 | dmg 4 · TowardCaster · kb 20 · stun 0.4 · snapZ | `PullEffect(r 3.5, f 12, dmg 4, stun 0.4)` |
| SK_TK1B | 대지 강타 | Push | Neutral → Knockback | Direction | 3 | 0.2 / 0.3 | dmg 12 · AwayFromCaster · kb 30 · stun 0.5 | `ChargeEffect(speed 20)` |
| SK_TK2A | 사슬 견인 | Gather | Neutral → LightHit | GroundPoint | 4.5 | 0.25 / 0.25 | dmg 4 · TowardCaster · kb 20 · stun 0.4 · snapZ | `PullEffect(r 4, f 12, dmg 5, stun 0.4)` |
| SK_TK3A | 방패 올려치기 | Launcher | LightHit → AerialHit | GroundPoint | 4 | 0.35 / 0.35 | dmg 12 · Up · launch 14 · stun 0.6 | `ChargeEffect(speed 20)` |

- 고유기(`Ally.selfSkill`): **SK_TK1B 대지 강타**
- TK1A는 자기 주변(`targeting: None` → Origin = 시전자), TK2A는 찍은 지점으로 텔포 후 흡입.

## 2. 전사 (Warrior) — role 1

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | radius | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|
| SK_WR1A | 돌진 베기 | Strike | Neutral → LightHit | Direction | 3 | 0.12 / 0.2 | dmg 7 · Fixed(정면) · kb 2 · stun 0.3 | `ChargeEffect(20)` + `ShieldEffect(30, 5s)` |
| SK_WR1B | 올려베기 | Launcher | LightHit → AerialHit | None | 3 | 0.12 / 0.25 · 간격 0.18 | 3타: 5 / 5 / **9** — 앞 2타는 Fixed kb 2, 3타만 Up launch 13 stun 0.6 | — |
| SK_WR3A | 회전 강타 | Push | AerialHit → WallBound | EnemyUnit | 3 | 0.25 / 0.3 | dmg 14 · AwayFromCaster · kb 30 · stun 0.5 | — (vfx shock 색 커스텀: 파랑) |
| SK_WR3B | 사슬 감아치기 | **Charge** | Neutral → AerialHit | None | 5 | 0.15 / 0.4 | dmg 14 · Up · launch 10 · stun 0.6 | `PullEffect(r 4, f 12, dmg 5, stun 0.4)` |

- 고유기: **SK_WR1A 돌진 베기**
- WR3B만 차징. `maxChargeTime 3` / `damageMul 3` / `radiusMul 2` (다른 스킬은 기본 2.5 / 2.5 / 1.6이지만 `IsCharge`가 아니라 미사용).

## 3. 궁수 (Archer) — role 2

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | 투사체 | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|
| SK_AR1A | 연속 사격 | Strike | Neutral → AerialHit | EnemyUnit | ○ spd 22 / range 10 | 0.15 / 0.2 · 간격 0.15 | 3타 · 각 dmg 5 · Fixed · kb 1.5 · stun 0.35 | — |
| SK_AR2A | 상승 화살 | Launcher | LightHit → AerialHit | EnemyUnit | ○ spd 22 / range 10 | 0.3 / 0.25 | dmg 6 · Up · launch 12 · stun 0.6 | — (manaCost **30**) |
| SK_AR2B | 강력 사격 | Push | AerialHit → WallBound | EnemyUnit | ○ spd 20 / range 12 | 0.3 / 0.3 | dmg 13 · AwayFromCaster · kb 30 · stun 0.5 | — |
| SK_AR3A | 그물사격 | Gather | Neutral → LightHit | GroundPoint (r 4) | ✕ | 0.3 / 0.2 | dmg 4 · TowardCaster · kb 30 · stun 0.4 · snapZ | `PullEffect(r 4, f 12, dmg 5, stun 0.4)` |

- 고유기: **SK_AR1A 연속 사격**
- AR3A는 `projectile`이 비어 있어 실제로는 근접 판정 + PullEffect다. "그물을 쏜다"는 연출과 어긋남 → 미해결.

## 4. 마법사 (Wizard) — role 3

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | radius / 투사체 | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|
| SK_WZ1A | 마력탄 연사 | Strike | Neutral → LightHit | Direction | ○ spd 24 / range 12 | 0.15 / 0.2 | dmg 9 · Fixed · kb 2 · stun 0.3 | — |
| SK_WZ2A | 융기 | Launcher | LightHit → AerialHit | GroundPoint | 4.5 | 0.35 / 0.3 | **hitDataList 비어 있음** — 판정 전부 Effect가 담당 | `AirborneEffect(r 4.5, launch 30, dmg 7, stun 0.6)` |
| SK_WZ2B | 중력장 | Gather | Neutral → LightHit | GroundPoint | 5 | 0.35 / 0.25 | dmg 5 · TowardCaster · kb 20 · stun 0.4 · snapZ | `PullEffect(r 4, f 12, dmg 5, stun 0.4)` |
| SK_WZ3A | 충격파 | Push | AerialHit → WallBound | Direction | 3.5 | 0.3 / 0.3 | dmg 14 · AwayFromCaster · kb **40** · stun 0.5 | — |

- 고유기: **SK_WZ1A 마력탄 연사**

---

## 5. 커버리지 검증

| AttackType | 탱커 | 전사 | 궁수 | 마법사 | 계 |
|---|---|---|---|---|---|
| Gather (모으기) | 2 | 0 | 1 | 1 | 4 |
| Launcher (띄우기) | 1 | 1 | 1 | 1 | 4 |
| Strike (공격기) | 0 | 1 | 1 | 1 | 3 |
| Push (밀치기) | 1 | 1 | 1 | 1 | 4 |
| Charge (차징) | 0 | 1 | 0 | 0 | 1 |
| **합** | **4** | **4** | **4** | **4** | **16** |

- ⚠️ **탱커에 Strike 없음, 전사에 Gather 없음.** 24종안의 "어느 직업 조합으로도 정석 체인 성립"은 깨졌다. 단일 직업으로 4슬롯 정석 체인이 되는 건 궁수 · 마법사뿐.
- 시동기(Launcher) 4장 = 덱 16장 중 4장. 손패 4장을 뽑을 때 시동기 0장이 나올 확률이 큼.
  `ComboCard.drawWeight`는 **현재 미사용** — `Deck.Draw`가 맨 위에서 순서대로 가져가므로 가중치가 개입할 자리가 없다. 셔플 단계에 편향을 주는 식으로 되살려야 함.
- 밀치기 3종(WR3A · AR2B · WZ3A)은 전부 `AerialHit → WallBound` 요구. TK1B만 `Neutral → Knockback`이라 지상에서도 쓸 수 있는 유일한 밀치기.

---

## 6. 코드와의 대조

### 6-1. `AttackType` enum — 실제 5개

```csharp
Gather, Launcher, Strike, Push, Charge
```

24종안에 있던 `둔화 / 도발 / 피해감소`는 enum에 없다. 해당 성격의 스킬도 에셋에 없다.
`AttackType`은 **`Charge` 하나만 동작이 갈린다** (`IsCharge` → `ChargeSkillState`). 나머지는 분류 태그일 뿐이고 실제 판정은 `hitDataList` + `effects`가 정한다.

### 6-2. `ISkillEffect` 구현체 — 7개 존재, 4개만 사용

| 클래스 | 사용처 |
|---|---|
| `PullEffect` | TK1A, TK2A, WR3B, AR3A, WZ2B |
| `AirborneEffect` | WZ2A |
| `ChargeEffect` | TK1B, TK3A, WR1A |
| `ShieldEffect` | WR1A |
| `TauntEffect` | **미사용** — 도발 스킬 없음 |
| `DamageCutEffect` | **미사용** |
| `LifestealEffect` | **미사용** |

`SlowEffect`는 존재하지 않는다(24종안에서 제안만 됐음).
24종안이 "`AirborneEffect`는 `HitData`로 충분하니 불필요"라 적었으나, 실제로는 **광역 띄우기**(WZ2A)에 필요해서 남아 있다. `HitData`는 단일 대상 판정이라 반경 안 다수를 못 잡는다.

### 6-3. `HitData.canOtg` — 필드는 있고, 쓰는 스킬은 없다

```csharp
public bool canOtg;   // 다운된 적에게도 적중하는 바닥쓸기(OTG) 판정인지
```

`CombatStateRules.Next` / `IsInvincible`가 이 필드를 읽어 다운 무적을 관통시킨다. 하지만 **16종 전부 `canOtg: 0`** — OTG 스킬(구 WR-06 처형)이 삭제되면서 경로가 비었다. 다운된 적은 현재 아무도 다시 띄울 수 없다.

### 6-4. `SkillData` 실제 필드 (24종안 템플릿과 다름)

```
skillName / icon / description
role / attackType
requireState / resultState
targeting / radius
castTime / hitInterval / recoveryTime
maxChargeTime / maxChargeDamageMul / maxChargeRadiusMul
projectile / projectileSpeed / projectileRange / projectilePierce
approachDistance          // 0이면 1.1 사용 (근접 직업 접근 거리)
hitDataList : List<HitData>
effects     : List<ISkillEffect>  (SerializeReference)
cooldown / manaCost
animation / vfx
```

- `TotalDuration = castTime + hitInterval × (히트수 - 1) + recoveryTime`
- `IsRanged`는 `projectile != null`로 판정 — `role`과 무관하다.

---

## 7. 미결정 · 어긋난 항목

- **밸런스 수치 미조정** — cooldown 5 / manaCost 20이 AR2A(30) 빼고 전부 동일. 사실상 미설정.
- **애니메이션 미연결** — 궁수 4종만 `animation`이 채워져 있는데, 그마저도 전용 클립이 아니라 공용 `AerialAttack.anim`이다. 나머지 12종은 `animation: {fileID: 0}`.
  직업별 스킬 클립 `SK_TK01 / SK_WR01 / SK_AR01 / SK_WZ01`은 만들어져 있으나 **어느 에셋 · 컨트롤러도 참조하지 않는다.**
- **AR3A 그물사격에 투사체 없음** — 원격 모으기인데 근접 판정으로 나감.
- **탱커 Strike / 전사 Gather 공백** — 5장 커버리지 표 참고.
- **`drawWeight` 미사용** — 시동기 보정이 실제로 안 걸린다.
- **`canOtg` 사용처 없음** — 다운 복구 콤보 루트 부재.
- **차징의 불릿타임 상호작용** — WR3B 하나뿐인 차징이 시간 정지 중에 어떻게 모이는지 미정.
- **벽바운드 판정 거리** — 맵 중앙에서 밀치기를 쓰면 벽에 안 닿아 그냥 넉백으로 끝난다.
- **미사용 Effect 3종**(`TauntEffect` / `DamageCutEffect` / `LifestealEffect`) — 살릴지 지울지 미정.

---

## 8. 신규 `SkillData` 에셋 템플릿

```
skillName     : 소용돌이 베기
icon          : (Assets/Sprites 의 스킬 아이콘)
description   : TK1A — 모으기 — 몸을 회전시켜 주변 적을 끌어모은다
role          : Tanker
attackType    : Gather
requireState  : Neutral
resultState   : LightHit
targeting     : None
radius        : 3.5
castTime      : 0.2
hitInterval   : 0.3
recoveryTime  : 0.25
hitDataList   : [ { damage: 4, nextState: LightHit, mode: TowardCaster,
                    knockbackForce: 20, hitStunDuration: 0.4, snapZ: true, canOtg: false } ]
effects       : [ PullEffect { radius: 3.5, force: 12, damage: 4, hitStun: 0.4 } ]
cooldown      : 5
manaCost      : 20
animation     : SK_TK01
```

명명 규칙: `SK_<직업2자><그룹번호><변형자>` — 예 `SK_WZ2B` = 마법사 · 2번 그룹 · B안.
