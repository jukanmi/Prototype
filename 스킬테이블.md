# 콤보 덱 스킬 테이블 (16종)

> 4직업 × 4종 = 16종. **에셋 1개 = 스킬 1개** (`Assets/Data/Skills/SK_*.asset`).
> 각 `Ally`가 자기 직업 4장을 `equipped`에 들고 있고, `BulletTimeController.BuildDeckFromParty()`가 이걸 모아 **16장 덱**을 만든다.
> 불릿타임마다 손패 **4장**(`Hand.Size`)까지 채우고, 손패 자체가 콤보 실행 순서다.
>
> 이 문서는 실제 에셋 값을 그대로 옮긴 것이다. 값을 고치려면 에셋을 고치고 여기를 다시 맞춘다.

---

## 0. 상태 전이 축

콤보는 `CombatState` 사슬 위에서 굴러감. 전이 판정은 `CombatStateRules`가 단독 관리.

```
Neutral ──모으기──> LightHit ──띄우기──> AerialHit ──밀치기──> Knockback ──벽접촉──> WallBound
                                            │                      │              │
                                        공격기(유지)                └──── 바닥접촉 ─┴──> Down ──> Getup(무적) ──> Neutral
                                                                                          ↑ canOtg 로만 관통
```

- 공중 피격 중에는 무엇을 맞아도 `AerialHit` 유지. **단 `Knockback` 요청만 통과** → 벽바운드 연계가 성립.
- `MaxAirHit = 10` 넘기면 띄우기 무시하고 낙하. 피격마다 중력 `+0.15`, 상한 `2.0`.
- `CanChain`: `require == AerialHit`이면 `AerialHit / WallBound / Knockback` 전부 허용.

**정석 4슬롯 체인**: `모으기(Gather) → 띄우기(Launcher) → 공격기(Strike) → 밀치기(Push)`

---

## 1. 탱커 (Tanker) — role 0

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | 조준 | radius | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|---|
| SK_TK소용돌이베기 | 소용돌이 베기 | Gather | Neutral → LightHit | None | 가까운 적 | 3.5 | 0.2 / 0.25 | dmg 12 · TowardCaster · kb 20 · stun 0.4 · snapZ | `PullEffect(r 3.5, f 12, dmg 14, stun 0.4)` |
| SK_TK방패올려치기 | 방패 올려치기 | Launcher | LightHit → AerialHit | GroundPoint | 가까운 적 | 4 | 0.35 / 0.35 | dmg 13 · Up · launch 14 · stun 0.6 | `ChargeEffect(speed 20)` |
| SK_TK방패연타 | 방패 연타 | Strike | AerialHit → AerialHit | GroundPoint | 가까운 적 | 4 | 0.12 / 0.25 · 간격 0.18 | 3타 · 각 dmg 12 · Fixed · kb 2 · stun 0.35 | — |
| SK_TK대지강타 | 대지 강타 | Push | AerialHit → WallBound | GroundPoint | **가장 먼 적** | 3 | 0.2 / 0.3 | dmg 40 · **TowardWall** · kb 30 · stun 0.5 · guard 3 | `ChargeEffect(speed 20)` |

- **사슬 견인(Gather)을 방패 연타(Strike)로 갈아끼웠다.** 탱커만 Gather가 둘이고 Strike가 없어
  이 직업 하나로는 정석 체인이 안 돌았다. 에셋을 지우지 않고 유형을 바꾼 이유는 씬의
  `Ally.equipped`가 GUID로 그 카드를 물고 있어서다 — 지우면 장착 슬롯에 null이 남는다.

## 2. 전사 (Warrior) — role 1

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | 조준 | radius | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|---|
| SK_WR사슬감아치기 | 사슬 감아치기 | Gather | Neutral → LightHit | None | 가까운 적 | 5 | 0.2 / 0.25 | dmg 14 · TowardCaster · kb 20 · stun 0.4 · snapZ | `PullEffect(r 4, f 32, dmg 5, stun 0.4)` |
| SK_WR올려베기 | 올려베기 | Launcher | LightHit → AerialHit | None | 가까운 적 | 3 | 0.12 / 0.25 · 간격 0.18 | 3타: 11 / 11 / **14** — 앞 2타는 Fixed kb 2, 3타만 Up launch 13 stun 0.6 | — |
| SK_WR돌진베기 | 돌진 베기 | Strike | Neutral → LightHit | Direction | 가까운 적 | 3 | 0.12 / 0.2 | dmg 35 · Fixed(정면) · kb 2 · stun 0.3 | `ChargeEffect(20)` + `ShieldEffect(30, 5s)` |
| SK_WR회전강타 | 회전 강타 | Push | AerialHit → WallBound | GroundPoint | **가장 먼 적** | 3 | 0.25 / 0.3 | dmg 40 · **TowardWall** · kb 30 · stun 0.5 · guard 3 | — |

- **사슬 감아치기를 Charge에서 Gather로 내렸다.** 전사만 Gather가 없었고, Charge는 정석 체인
  네 칸 밖에 있는 유형이라 손패에 잡히면 그 칸이 통째로 놀았다. `ChargeSkillState` 코드는
  남아 있다 — 되살릴 때 유형만 되돌리면 된다.

## 3. 궁수 (Archer) — role 2

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | 조준 | 투사체 | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|---|
| SK_AR그물사격 | 그물사격 | Gather | Neutral → LightHit | GroundPoint (r 4) | 가까운 적 | ✕ | 0.3 / 0.2 | dmg 14 · TowardCaster · kb 30 · stun 0.4 · snapZ | `PullEffect(r 4, f 12, dmg 10, stun 0.4)` |
| SK_AR상승화살 | 상승 화살 | Launcher | LightHit → AerialHit | GroundPoint | 가까운 적 | ○ spd 22 / range 10 | 0.3 / 0.25 | dmg 14 · Up · launch 12 · stun 0.6 | — (manaCost **30**) |
| SK_AR연속사격 | 연속 사격 | Strike | Neutral → AerialHit | GroundPoint | 가까운 적 | ○ spd 22 / range 10 | 0.15 / 0.2 · 간격 0.15 | 3타 · 각 dmg 34 · Fixed · kb 1.5 · stun 0.35 | — |
| SK_AR강력사격 | 강력 사격 | Push | AerialHit → WallBound | GroundPoint | **가장 먼 적** | ○ spd 20 / range 12 | 0.3 / 0.3 | dmg 40 · **TowardWall** · kb 30 · stun 0.5 · guard 3 | — |

- AR 그물사격은 `projectile`이 비어 있어 실제로는 근접 판정 + PullEffect다.
  "그물을 쏜다"는 연출과 어긋남 → 미해결.

## 4. 마법사 (Wizard) — role 3

| 에셋 | 이름 | 유형 | 선행 → 결과 | 타겟팅 | 조준 | radius / 투사체 | 선딜/후딜 | 히트 | Effects |
|---|---|---|---|---|---|---|---|---|---|
| SK_WZ중력장 | 중력장 | Gather | Neutral → LightHit | GroundPoint | 가까운 적 | 5 | 0.35 / 0.25 | dmg 15 · TowardCaster · kb 20 · stun 0.4 · snapZ | `PullEffect(r 4, f 12, dmg 12, stun 0.4)` |
| SK_WZ융기 | 융기 | Launcher | LightHit → AerialHit | GroundPoint | 가까운 적 | 4.5 | 0.35 / 0.3 | dmg 12 (부가 히트) | `AirborneEffect(r 4.5, launch 30, dmg 13, stun 0.6)` |
| SK_WZ마력탄연사 | 마력탄 연사 | Strike | Neutral → LightHit | Direction | 가까운 적 | ○ spd 24 / range 12 | 0.15 / 0.2 | dmg 37 · Fixed · kb 2 · stun 0.3 | — |
| SK_WZ충격파 | 충격파 | Push | AerialHit → WallBound | GroundPoint | **가장 먼 적** | 3.5 | 0.3 / 0.3 | dmg 40 · **TowardWall** · kb 40 · stun 0.5 · guard 3 | — |

- 마법사 4종이 **훈련장 견본 체인**이다(`Stage_Training`). 넷 다 원거리라 시전자가
  카메라 밖으로 나가지 않아 한 싸이클이 화면 안에서 끝난다.

---

## 4-1. 밀치기(Push) 공통 규칙

밀치기는 네 직업 모두 **같은 규칙 한 벌**을 쓴다. 유형이 곧 규칙이다.

| 항목 | 값 | 이유 |
|---|---|---|
| `mode` | `TowardWall` | 시전자 기준(`AwayFromCaster`)이면 시전자가 어디 섰느냐에 따라 적이 방 한복판으로 날아가 벽바운드가 운에 맡겨진다. 방향은 **가장 가까운 벽**이 정한다. |
| `targetPick` | `Farthest` | 코앞의 적을 밀면 한 뼘 가서 벽에 닿고 끝난다. 벽까지 밀 거리가 나오려면 가장 먼 적이어야 한다. |
| `damage` | 40 | 체인의 마무리다. 공격기(35~37)보다 낮으면 마무리를 넣을 이유가 없다. |
| `guardDamage` | 3 | 보스 가드를 허무는 역할. 비워 두면 평타와 같은 한 대분만 깎여 "가드 브레이커"라는 정체성이 사라진다. |
| `nextState` | `Knockback` | 벽바운드는 **벽에 닿아야** 생긴다(`CombatStateRules.OnWallContact`). 여기에 WallBound를 적으면 벽 없이 상태만 바뀐다. |

방향 계산은 `WallFinder`가 **좌우(±X)로만** 레이를 쏴 가까운 벽을 고른다.
앞뒤(±Z)는 슬램할 벽이 아니라 레인 경계라 뺐다 — 방이 Z로 좁아(±3.25) 그대로 두면
웬만한 자리에서 밀치기가 항상 화면 안쪽 벽으로 간다.
실전(`Combat.ApplyKnockback`)과 프리뷰(`KnockbackPreview`)가 **같은 함수**를 부른다 —
두 벌로 갈리면 화살표가 곧 거짓말이 된다. 벽을 못 찾으면 예전처럼 시전자 반대쪽으로 떨어진다.

---

## 5. 미결정 · 어긋난 항목

- **밸런스 수치 미조정** — cooldown 5 / manaCost 20이 AR2A(30) 빼고 전부 동일. 사실상 미설정.
- **애니메이션 미연결** — 궁수 4종만 `animation`이 채워져 있는데, 그마저도 전용 클립이 아니라 공용 `AerialAttack.anim`이다. 나머지 12종은 `animation: {fileID: 0}`.
  직업별 스킬 클립 `SK_TK01 / SK_WR01 / SK_AR01 / SK_WZ01`은 만들어져 있으나 **어느 에셋 · 컨트롤러도 참조하지 않는다.**
- **AR3A 그물사격에 투사체 없음** — 원격 모으기인데 근접 판정으로 나감.
- **`drawWeight` 미사용** — 시동기 보정이 실제로 안 걸린다.
- **`canOtg` 사용처 없음** — 다운 복구 콤보 루트 부재.
- **차징 유형 보류** — 유일한 차징이던 WR 사슬 감아치기를 Gather로 내렸다. `ChargeSkillState` · `ChargeGauge`는 남아 있고 쓰는 에셋만 없다.
- ~~**벽바운드 판정 거리**~~ — 해결. 밀치기가 `TowardWall`로 항상 가장 가까운 벽을 향한다(4-1장).
- **미사용 Effect 3종**(`TauntEffect` / `DamageCutEffect` / `LifestealEffect`) — 살릴지 지울지 미정.

---

## 6. 신규 `SkillData` 에셋 템플릿

```
skillName     : 소용돌이 베기
icon          : (Assets/Sprites 의 스킬 아이콘)
description   : TK1A — 모으기 — 몸을 회전시켜 주변 적을 끌어모은다
role          : Tanker
attackType    : Gather
requireState  : Neutral
resultState   : LightHit
targeting     : None
radius        : 3.5
castTime      : 0.2
hitInterval   : 0.3
recoveryTime  : 0.25
hitDataList   : [ { damage: 4, nextState: LightHit, mode: TowardCaster,
                    knockbackForce: 20, hitStunDuration: 0.4, snapZ: true, canOtg: false } ]
effects       : [ PullEffect { radius: 3.5, force: 12, damage: 4, hitStun: 0.4 } ]
cooldown      : 5
manaCost      : 20
animation     : SK_TK01
```

명명 규칙: `SK_<직업2자><그룹번호><변형자>` — 예 `SK_WZ2B` = 마법사 · 2번 그룹 · B안.
